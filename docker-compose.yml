services:
  app:
    build: .
    container_name: mariage-sami-prescilia
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      # Persist RSVP data across container restarts
      - ./server/data:/app/server/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
  # Nginx reverse proxy (optionnel si tu utilises nginx-proxy sur ton VPS)
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/conf.d/default.conf
  #   depends_on:
  #     - app
